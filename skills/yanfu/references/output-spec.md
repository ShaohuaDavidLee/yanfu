# 输出规范

## 输出目录

生成：

```text
output/
├── landing.html
├── yanfu-notes.md
└── assets/                 # 只有需要本地资源时才创建
```

只要最终目录可整体移动且所有路径有效，`landing.html` 也可以引用 `assets/` 之外的用户素材。

## landing.html

要求：

- 单一、响应式 HTML 入口。
- 包含 `lang` 属性和 viewport 元数据。
- 只有一个 H1。
- 首屏说清产品身份、用户价值和主要 CTA。
- 忠于原 Landing Page 的设计风格，保留原品牌气质。用户提供的产品截图默认只作为事实证据，不直接进入页面。
- 主要链接和本地资源可用。
- 不显示内部置信度、翻译分析、证据归因、截图说明或“为什么这样改”的说明文字。
- 不发明数据、评价、客户 Logo 或产品能力。
- 不要重新设计。除非原页面存在明显的移动端溢出、遮挡、不可读或无障碍问题，否则不要改变视觉方向、主色、字体气质、组件语言和整体视觉密度。
- `landing.html` 面向目标用户，应像真实可上线页面；不得写成审查报告、案例复盘、翻译说明或证据展示页。
- 只有当原 Landing Page 本身已经使用产品截图作为核心销售素材，或用户明确要求在页面中展示截图，或不展示截图会导致产品形态无法理解时，才可克制展示截图。展示时避免“截图中可见”“界面证据”等说明，改用用户利益导向的正常产品预览文案。
- 普通 HTML/CSS 足够时，不引入不必要的框架和构建步骤。

具体 Section 顺序由证据决定，优先采用：

> Hero → 用户处境 → 产品机制 → 用户结果 → 信任/证明 → FAQ → CTA

## yanfu-notes.md

使用以下结构：

```markdown
# 严复译注

## 信
- 原始价值主张：
- 忠实保留：
- 表达依据：
- 因证据不足未使用：

## 达
- 最终一句话 Pitch：
- 故事主线：
- 信息顺序调整：
- 删除、合并或弱化：

## 电梯演讲
- 30 秒版本：
- 使用场景：
- 不应越界承诺：

## 雅
- 保留的设计元素：
- 产品截图选择：
- 忠于原 Landing Page 的设计风格：
- 必要且克制的视觉修正：

## 待确认
- 需要开发者确认：
- 暂未进入页面：
```

译注保持简洁。没有内容的项目写 `无`。

## 完成时的回复

依次返回：

1. `landing.html` 的预览链接或路径。
2. `yanfu-notes.md` 的路径。
3. 一句话说明最高优先级的待确认内容；没有时明确说明无需确认。

不得用长篇评价代替文件交付。
